git init //Se instala en la carpeta .get 
git status//que carpetas hay en el proyecto
git add nombredelachivo.txt// git add .//agrega todo los archivos del proyecto  
git commit// hace cambio con la persona
git commit -m "se agrega el comentario"//mas facil y rapido 
git config --global user.name "nombre"// Se configura el nombre 
git config --global user.email "correo@"// se configura el correo
git log // Mustra el historial
git diff archivo.ejemplo // Muestra el cambio que se le hace al archivo el codigo
git branch login // Se crea una version cuando se cre una version diferen uno puede modificar de manera libre
git brach // Mustra la versiones que esta el proyecto
git checkout master// se cambia a la rama master
git remote add origin https://github.com/Ediel96/axmple-login-git.git // ejemplo de la direccion para poder subir el repositorio
git push -u origin master // se confirma para poider subir el repositorio



------- para ignora un archivo ---------
se crea un archivo .gitignore en ese mismo archivo se coloca el nombre del archo en el cual se quiere ignorar
-- carpeta "nombredelacarpeta"
-- archovo de texto "nombredelarchivo.txt"

